import Casefile from "@/components/Casefile";

export const metadata = {
  title: "Daily Suspect — Casefile",
  description: "A long-form closed-circle murder mystery. Interrogate, cross-examine, deduce.",
};

export default function Page() {
  return <Casefile />;
}
